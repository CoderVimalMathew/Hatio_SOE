package com.example.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
This module is used as the main file to initiate the spring-boot application.
*/
@SpringBootApplication
public class HatioChallengeApplication {
  /**
  This is the main class where the spring application starts running.
  */
  public static void main(String[] args) {
    SpringApplication.run(HatioChallengeApplication.class, args);
  }
}
